package com.connectsphere.post.enums;

/**
 * Indicates whether a post has media attachments.
 * TEXT  — text-only post
 * MEDIA — post with one or more image/video URLs
 */
public enum PostType {
    TEXT,
    MEDIA
}